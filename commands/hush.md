---
description: Silence Bolo immediately. Kills any audio currently playing AND skips the next auto-read. Five-keystroke shortcut for `/speak hush`.
---

## Your Task

Run Bolo's hush flag — kill in-flight audio + suppress next auto-read in one shot.

```bash
"${BOLO_HOME:-$HOME/.local/share/bolo}/bin/bolo" --hush
```
